---
- name: Deploy Cilium
  kubernetes.core.helm:
    name: cilium
    chart_ref: "{{ cilium_chart_registry }}"
    release_namespace: kube-system
    chart_version: "{{ cilium_chart_version }}"
    kubeconfig: "{{ kubeconfig }}"
    values:
      autoDirectNodeRoutes: true
      hostFirewall:
        enabled: true
      ipam:
        mode: kubernetes
      kubeProxyReplacement: "true"
      k8sClientRateLimit:
        burst: 64
        qps: 32
      k8sServiceHost: "{{ hostvars[groups['server'][0]]['ansible_host'] }}"
      k8sServicePort: "6443"
      l2announcements:
        enabled: true
      routingMode: native
      ipv4NativeRoutingCIDR: "{{ hostvars[groups['server'][0]]['k3s_cluster_cidr'] }}"
  when: hostvars[groups['server'][0]]['enable_cilium']

- name: Create manifests needed for Cilium
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('template', item) | from_yaml }}"
    kubeconfig: "{{ kubeconfig }}"
  loop:
    - "cilium-lb-ip-pool.yaml"
    - "cilium-l2-announcement-policy.yaml"
  when: hostvars[groups['server'][0]]['enable_cilium']
