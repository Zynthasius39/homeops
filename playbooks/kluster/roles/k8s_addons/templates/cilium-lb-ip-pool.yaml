---
apiVersion: "cilium.io/v2"
kind: CiliumLoadBalancerIPPool
metadata:
  name: lb-pool
spec:
  allowFirstLastIPs: "No"
  blocks:
    - cidr: "{{ hostvars[groups['server'][0]]['k3s_loadbalancer_cidr'] }}"
