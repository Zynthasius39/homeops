# ------------------------------------------------------------
# gitea_ssh
# ------------------------------------------------------------

upstream gitea_ssh {
  server 10.0.50.31:22;
}

server {
  listen 22;
  proxy_pass gitea_ssh;
  proxy_timeout 600s;
  proxy_connect_timeout 5s;
}
